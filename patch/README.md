# Patch 目录

本目录用于沉淀必须作用于 `bk_avdk_smp` 的补丁和说明。

原则：

- 优先在 `projects/cmaiw82al_ai_toy` 内完成产品适配。
- 只有 SDK bug、底层驱动缺口或上游组件无法通过工程配置解决时，才改 SDK。
- 每个补丁应记录适用 SDK commit/分支、原因、变更文件、验证命令和回滚方式。
- 不要只把 SDK 改动留在工作区；后续同步 SDK 时会丢失上下文。
